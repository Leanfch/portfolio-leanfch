---
// CHECKPOINT 4 — ProjectCard: screenshot, nombre, tags, descripción, botones
import type { Project } from '../data/projects';

interface Props {
  project: Project;
}

const { project } = Astro.props;
---

<article>
  <!-- {project.title} -->
</article>
