---
// CHECKPOINT 4 — CaseStudy: contexto, rol, stack, decisiones, métricas
import type { Project } from '../data/projects';

interface Props {
  project: Project;
}

const { project } = Astro.props;
---

<div>
  <!-- Case study: {project.title} -->
</div>
